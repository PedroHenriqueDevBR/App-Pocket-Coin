<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.droppages.pedrohenrique.pocketcoin">

    <application
        android:name=".dal.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TagActivity"></activity>
        <activity android:name=".LogActivity" />
        <activity android:name=".view.CategoriaActivity" />
        <activity android:name=".view.CarteiraActivity" />
        <activity android:name=".view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.CadastrarUsuarioActivity" />
        <activity android:name=".view.MainActivity" />
    </application>

</manifest>